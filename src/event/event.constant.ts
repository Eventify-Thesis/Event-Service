export enum EventType {
  'ONLINE' = 'Online',
  'OFFLINE' = 'Offline',
}

export enum EventStatus {
  DRAFT = 'Draft',
  PUBLISHED = 'Published',
  INCOMING = 'Incoming',
  WATING_FOR_APPROVAL = 'Waiting for approval',
}
